{
  "name": "stereoframe",
  "private": true,
  "version": "0.1.0",
  "license": "Apache-2.0",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "bun run --cwd packages/runtime build && bun run --cwd packages/cli build",
    "test": "bun run --cwd packages/runtime test"
  }
}
