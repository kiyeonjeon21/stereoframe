{
  "name": "stereoframe",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "bun run --cwd packages/runtime build && bun run --cwd packages/cli build",
    "test": "bun run --cwd packages/runtime test && bun run --cwd packages/cli test",
    "check": "bun scripts/check.mjs",
    "release": "bun scripts/release.mjs"
  }
}
